{
  "name": "ai-ml",
  "private": true,
  "scripts": {
    "build": "npm run build:cloudflare",
    "build:cloudflare": "npm ci --prefix web-presentation && cross-env CLOUDFLARE=1 npm run build --prefix web-presentation",
    "deploy:workers": "npx wrangler deploy -c wrangler.worker.jsonc",
    "deploy:cloudflare": "npm run build:cloudflare && npm run deploy:workers"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "wrangler": "^4.14.0"
  }
}
